import React from "react";
import { Title, Description } from "./infoBoatId.styled";

const InfoBoatId = ({title, description}) => (
    returm(
        <>
            <Title>{title}</Title>
            <Description>{description}</Description>
        </>
    )
)

export default InfoBoatId